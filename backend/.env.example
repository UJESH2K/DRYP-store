# Database URL (MongoDB)
# JWT secret for legacy tokens
# Supabase (primary auth)
# SMTP (Gmail)
# AWS S3 (product images)
# OpenAI (stylist + embeddings)
# Shopify OAuth
# Razorpay (payments)
# Razorpay key (public, used by frontend checkout)
